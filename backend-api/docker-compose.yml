# Optional: Docker Compose for PostgreSQL
# Use this if you prefer Docker over manual PostgreSQL installation

version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: learnai-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: learnai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Optional: Run the API in Docker as well
  # api:
  #   build: .
  #   container_name: learnai-api
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/learnai
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - ./models:/app/models

volumes:
  postgres_data:
