# =============================================================================
# LearnAI Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# FRONTEND (Vite)
# -----------------------------------------------------------------------------
# API URL for the FastAPI backend
VITE_API_URL=http://localhost:8000/api

# WebSocket URL for real-time features (optional)
VITE_WS_URL=ws://localhost:8000

# -----------------------------------------------------------------------------
# BACKEND (FastAPI) - These are read by backend-api/.env
# -----------------------------------------------------------------------------
# PostgreSQL Connection String
# Format: postgresql://username:password@host:port/database
# IMPORTANT: Replace YOUR_PASSWORD with your actual PostgreSQL password
DATABASE_URL=postgresql://postgres:12345678@localhost:5432/ai_tutor

# JWT Authentication
# IMPORTANT: Generate a secure key for production: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=146b83620aacd1c63f52a080edf2992fdf4a6b4ff8453741621571cd88879d1b
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080

# -----------------------------------------------------------------------------
# Google OAuth 2.0
# -----------------------------------------------------------------------------
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Go to APIs & Services > Credentials
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:8000/api/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Email / SMTP Configuration
# -----------------------------------------------------------------------------
# For Gmail: Use an App Password (not your regular password)
# 1. Enable 2FA on your Google account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587backend-api/models/Phi-3-mini-4k-instruct-q4.gguf
voice-model/datasets/data/LJSpeech-1.1.tar.bz2
voice-model/models/checkpoints/checkpoint_epoch_1.pt
voice-model/models/checkpoints/interrupted_checkpoint.pt
voice-model/models/output/best_model.pt
SMTP_USER=esthermuthoni3001@gmail.com
SMTP_PASSWORD=eazc hivh uhbd sjqd
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=LearnAI

# Verification token expiry (in hours)
VERIFICATION_TOKEN_EXPIRE_HOURS=24

# Debug mode (set to False in production)
DEBUG=True
